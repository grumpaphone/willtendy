'use client';

import { Container } from '@mantine/core';

export function Header() {
	return (
		<header>
			<Container>{/* Empty header - logo is now in the home page */}</Container>
		</header>
	);
}
